# AddLogementDialog - Modal d'ajout de logement

Ce projet contient uniquement la modal **AddLogementDialog** extraite du projet principal, avec toutes ses dépendances.

## 🎯 Fonctionnalités

La modal permet de créer un logement en 5 étapes :

1. **Informations de base** : Nom, adresse, lien Airbnb (optionnel)
2. **Type de parcours** : Ménage ou Voyageur
3. **Sélection du modèle** : Modèle Check Easy ou personnalisé
4. **Chargement Airbnb** : Extraction automatique des pièces et photos (si lien fourni)
5. **Sélection des pièces** : Choix manuel ou validation des pièces Airbnb
6. **Ajout de photos** : Upload de photos pour chaque pièce (si pas Airbnb)

## 📦 Structure du projet

```
add-logement-modal/
├── src/
│   ├── components/
│   │   ├── logements/
│   │   │   ├── AddLogementDialog.tsx       # Modal principale
│   │   │   ├── AddPhotosDialog.tsx         # Étape photos
│   │   │   ├── AirbnbLoadingDialog.tsx     # Chargement Airbnb
│   │   │   └── AirbnbResultDialog.tsx      # Résultats Airbnb
│   │   ├── parcours/
│   │   │   └── dialogs/
│   │   │       ├── SelectModeleDialog.tsx  # Sélection modèle
│   │   │       └── SelectPiecesDialog.tsx  # Sélection pièces
│   │   └── ui/                             # Composants shadcn-ui
│   ├── types/
│   │   └── modele.ts                       # Types TypeScript
│   ├── assets/
│   │   └── airbnb-logo.png                 # Logo Airbnb
│   ├── lib/
│   │   └── utils.ts                        # Utilitaires
│   ├── hooks/
│   │   └── use-toast.ts                    # Hook toast
│   ├── App.tsx                             # Exemple d'utilisation
│   ├── main.tsx                            # Point d'entrée
│   └── index.css                           # Styles globaux
├── package.json
├── vite.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

## 🚀 Installation

```bash
cd add-logement-modal
npm install
```

## 💻 Lancement

```bash
npm run dev
```

L'application sera accessible sur `http://localhost:8080`

## 📝 Utilisation

```tsx
import { AddLogementDialog } from "@/components/logements/AddLogementDialog";
import { ParcoursModele } from "@/types/modele";

function MyComponent() {
  const [dialogOpen, setDialogOpen] = useState(false);
  const [customModeles, setCustomModeles] = useState<ParcoursModele[]>([]);

  const handleComplete = (data) => {
    console.log("Logement créé:", data);
    // data contient: nom, adresse, parcoursType, modele, pieces, piecesPhotos
  };

  const handleCreateCustom = (parcoursType: "menage" | "voyageur") => {
    // Gérer la création d'un modèle personnalisé
  };

  return (
    <>
      <button onClick={() => setDialogOpen(true)}>
        Ajouter un logement
      </button>

      <AddLogementDialog
        open={dialogOpen}
        onOpenChange={setDialogOpen}
        customModeles={customModeles}
        onCreateCustom={handleCreateCustom}
        onComplete={handleComplete}
      />
    </>
  );
}
```

## 🔧 Props de AddLogementDialog

| Prop | Type | Description |
|------|------|-------------|
| `open` | `boolean` | État d'ouverture de la modal |
| `onOpenChange` | `(open: boolean) => void` | Callback de changement d'état |
| `customModeles` | `ParcoursModele[]` | Liste des modèles personnalisés |
| `onDeleteCustom` | `(id: string) => void` | Callback de suppression de modèle |
| `onEditCustom` | `(modele: ParcoursModele) => void` | Callback d'édition de modèle |
| `onCreateCustom` | `(type: "menage" \| "voyageur") => void` | Callback de création de modèle |
| `onComplete` | `(data: LogementData) => void` | Callback de complétion |

## 🛠️ Technologies

- **React 18** avec TypeScript
- **Vite** pour le build
- **Tailwind CSS** pour le styling
- **shadcn-ui** pour les composants UI
- **Lucide React** pour les icônes
- **Zod** pour la validation

## 📄 Licence

Ce composant est extrait du projet PARCOUR APP.

